import { IntegrationsView } from "@/views/admin/integrations";

export const metadata = { title: "Integrations · Admin" };

export default function AdminIntegrationsPage() {
  return <IntegrationsView />;
}